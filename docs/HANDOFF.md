# Active Handoff Contract

Last reconciled: 2026-07-20

This document is the short-lived execution contract for the current gate. It
may narrow the active slice but may never expand or contradict
[AGENTS.md](../AGENTS.md), the [Vision](VISION.md), the
[glossary](../CONTEXT.md), the [Architecture](ARCHITECTURE.md), the
[Roadmap](ROADMAP.md), or the active implementation contract.

Update this file whenever the active gate, permitted scope, known proof,
blocker, UAT result, or next forbidden work changes.

## Current gate

**Goal 4B complete — awaiting separate 4C authorization**

- Goal 2 and both Slice 3 gates passed automated proof and player UAT.
- Gate 3B's accepted player-view baseline remains `20 px`; Goal 4B's enlarged
  canvas presents that baseline at its current native density below.
- The player authorized Goal 4 on 2026-07-19, beginning with 4A.
- Slice 4A implementation, the full automated gate, and every player UAT item
  passed on 2026-07-19.
- The Goal 4 contract, Roadmap, UAT sheet, and this Handoff are reconciled.
- The accepted UAT carries one non-blocking layout note: the text/menu regions
  are much larger than the playspace. The next authorized UI-bearing slice
  must budget for a substantially larger world view before adding more
  interface surface; this note does not itself authorize that work.
- Slice 4B implementation and its full automated proof completed on
  2026-07-19. The player reported every functional UAT item passed on
  2026-07-20, then found the Clock speed line covered by the Codex panel.
- The specific overlap correction compacts Tick and Clock onto one line and
  adds a headless four-line layout assertion. Focused two-process 4B proof
  passes, and the complete retained verifier passed again on 2026-07-20 with
  zero errors. Its only warning was `NU1900` because the NuGet vulnerability
  feed was unreachable. The player confirmed the corrected layout looks good.
- `HERE — BUILD` grants and equips authored `Found` without making Build a
  class: a mixed Codex can independently equip `Fly` or `Found`.
- The fixed seed-`41337` fixture rejects flooded Stone at `surface (0, 0)` and
  founds the singular **THE FIRST HEARTH** at soil-supported Stone on
  `surface (0, 3)`, creating **The First Hearthstone** without moving the
  Incarnation.
- Core's physical Return Route reports X-before-Y ordinary steps and never
  moves the player. Strict current save envelope `3` preserves canonical Home
  state; literal v2, v1, and pre-envelope saves remain predecessor migrations.
- The current `1600 × 900` canvas has a `1020 × 740` map: `20 px` presents
  `51 × 37` cells and retained `16 px` presents `63 × 45`; the automated
  acceptance marker intentionally retains its `view=50x36` minimum.
- Non-blocking UAT notes are retained without expanding 4B: consider an
  optional roughly 30% closer camera in a later comparison, require an
  appropriate future capability for water traversal, and consider a
  directional Return Route arrow in a later UI-bearing slice.
- Goal 4 remains three sequential UAT slices. Slice 4B is accepted and
  reconciled. Slice 4C is the next candidate but stays forbidden until
  separately authorized.
- The active contract is
  [Goal 4 — Three Openings, One Chronicle](GOAL-4-THREE-OPENINGS.md).
- P-GEN remains an optional parallel authoring compiler. Its E4.5 correction
  did not block 4B, and any future E5 adoption remains outside this workspace's
  active production scope.
- The read-only [P-GEN E4.5 readiness review](P-GEN-E4-5-READINESS-REVIEW.md)
  reproduced its deterministic compiler and Godot proof. It did not adopt the
  pack. A future E5 gate must separately settle the Palimpsest-owned reader,
  required-vocabulary synchronization, motif placement, cosmetic selection,
  authoritative pack profile, negative fixtures, and visual UAT.

## Objective now

Goal 4B is closed. No production slice is active.

Slice 4C — A Consequential Fight is the next Roadmap candidate. Before any
production change:

1. obtain separate player authorization for 4C;
2. narrow it to one exact deterministic, pausable conflict fixture, its public
   seams, persistent consequence, automated proof, and player UAT; and
3. update the Goal 4 contract and this Handoff with that permitted boundary.

The accepted 4B camera-detail, water-traversal, and route-arrow notes remain
future considerations; they are not automatically part of 4C. P-GEN adoption
also remains a separate future decision.

## Permitted scope

Documentation review, scoping discussion, and an explicit 4C authorization
decision are permitted. No new production behavior is permitted. If 4C is
authorized, update this Handoff before implementation. Preserve the completed
4A and 4B Core, Visual, Inspector, Godot, migration, and player-UAT evidence.

## Retained 4A player-visible regression journey

1. Choose the `UP` Explore opening and receive `Fly`.
2. Reach The Bell That Fell Up.
3. Examine its generated sky-stone Study Source.
4. See two ordered plausible words and distinct contextual reasons.
5. Deliberately choose one word.
6. See only that word receive partial Understanding.
7. Pause and prove Study stops.
8. Save and load exact partial progress and selection.
9. End the body at the Bell and create a replacement.
10. See the Codex and all partial Understanding survive while the replacement
    has a fresh empty Loadout.
11. Re-equip `Fly`, return, deliberately resume the same word, and complete it.
12. See the chosen word enter the Codex exactly once.
13. Complete the accepted `Fly[Stone]`, material-change, death/replacement, and
    next-launch regression journey.

## Automated proof completed

- unique, stable catalogue identities and correct kinds/compatibilities;
- one deterministic, pure Bell source and no source away from its generated
  semantic identity;
- exact ordered offer identities, rationales, qualities, and yields;
- rejection of stale, absent, unknown, learned, and unoffered choices;
- only the selected word advances;
- switching preserves each word's accumulated Understanding;
- pause and leaving-source behavior;
- exact partial save/load;
- death clears active Study but preserves Understanding;
- replacement keeps Codex/Understanding and resets the Loadout;
- completion caps progress and learns once;
- generated catalogue/source snapshots remain out of saved Chronicle state;
- deterministic replay and query-order neutrality;
- literal Slice 0, Slice 1, 2A, 2B, and version-1 migration;
- unchanged physical Surface/Sky semantics for migrated version `1`
  Chronicles, unchanged legacy version `0` physical semantics, and every
  earlier regression;
- focused Godot control journey, layout fit, application restart, and no
  background Godot process or temporary verifier residue.
- compact four-line Chronicle header with Clock visible above Codex in the 4B,
  Gate 3B, and retained Slice 2C player paths.

Goal 4B additionally proves the real two-process Home journey: the first
isolated Godot process chooses `HERE`, rejects the flooded Stone at
`surface (0, 0)`, founds at `surface (0, 3)`, saves an explicit Home in strict
envelope v3, and leaves by ordinary movement. The verifier reads that isolated
save to require v3 and explicit Home before a second process restores it. The
restart process follows the physical route home and proves the exact
Home/Hearthstone/route facts plus the required acceptance marker; Core proof
retains death/replacement and literal v2, v1, and pre-envelope migration
coverage.

The complete post-implementation verifier passed on 2026-07-19 and passed
again after the Clock/Codex correction on 2026-07-20:

```text
GOAL4A CORE ACCEPTANCE PASS catalogue=Fly,Stone,Bell source=Stone>Bell stone=16 bell=16 save=2
GOAL4A ACCEPTANCE PASS stoneBranch=16 bellBranch=16 finalStone=0 finalBell=16 evidence=stone-replacement,bell-save-load
PASS: Gate 3A Core World Grammar plus Slice 0 through Slice 2C regression and save compatibility verified.
GOAL4B ACCEPTANCE PASS home=surface:0,3 material=hearthstone route=physical view=50x36 save=3
PASS: Goal 4B Home and restart acceptance, Goal 4A Study choice, every Goal 2 regression, Gate 3A Inspector, and Gate 3B visual acceptance verified.
```

The gate retains the partial/completed 4A Study restarts and the isolated 4B
save/restart boundary above. The correction run completed with zero errors;
its only warning was `NU1900` when the NuGet vulnerability feed was
unreachable.

## Do not drift into

- any new 4B production behavior before the player identifies a specific UAT
  failure;
- Slice 4C health, damage, enemies, combat, loot, or bestiary work;
- additional Starting Vectors before the next slice is explicitly authorized;
- more than one Study Source or more than the confirmed `Fly`, `Stone`, and
  `Bell` fixture;
- generated word meanings, catalogue JSON, tags, handler registries, plugin
  systems, source depletion, irreversible allocation, or generic content
  authoring;
- a Godot-owned catalogue, offer list, eligibility rule, yield, compatibility
  rule, or persistence decision;
- World Grammar terrain refinement, visual-art expansion, P-GEN integration,
  runtime compilation, or E5;
- factions, Agents, residents, production, jobs, Pressures, raids, Chronicle
  Records, Palimpsests, Decrees, or World Claims;
- a general migration framework, event sourcing, database, network service, or
  interface justified by one concrete implementation.

## Known proof retained

- `checks/verify.ps1` passed the full Goal 4B, Goal 4A, and retained regression
  gate again on 2026-07-20 after the Clock/Codex correction, ending with the
  exact Goal 4B Home/restart terminal line and zero errors. `NU1900` was the
  only warning because the NuGet vulnerability feed was unreachable.
- The player confirmed every 4B functional UAT item and the focused
  Clock/Codex correction recheck passed on 2026-07-20.
- The player reported that every item in the 20-pixel Goal 4A UAT passed on
  2026-07-19.
- Chronicle.Core, Chronicle.VisualPack, and Chronicle.Visuals build and test
  without Godot.
- The Core harness retains deterministic generation, fixed ticks, Study,
  Loadout, Expression, Home/Return Route, death/replacement, v3 current and
  predecessor-save migration, replay, and numeric-address checks.
- The Visual harness retains both pack sizes, mapping, adjacency, composition,
  stable variants, overlap, render digests, and numeric-address checks.
- Godot's complete Goal 2 journey, Gate 3A Inspector, Gate 3B player and
  Inspector paths, editor callback, save isolation, and next-launch restore
  pass.
- Slice 3's semantic and visual limitations remain recorded evidence. Goal 4B
  does not authorize changing physical geography or hiding limitations behind
  new art.
- P-GEN's external E4.5 verifier passed from a clean worktree on 2026-07-20
  with zero warnings or errors, byte-identical canonical output, aggregate
  digest
  `sha256:f41d1e4e4f76b5e6e57921cda35050582368486e87e932d5f1273ff4c2be9bd8`,
  and matching Godot/CPU-oracle captures. This is retained readiness evidence,
  not production adoption or player UAT.

Exact commands and packaged executable paths remain in the
[Development guide](DEVELOPMENT.md).

## Stop and hand off

Slices 4A and 4B are accepted and reconciled. Stop here. The next task may
review and authorize Slice 4C, but must not implement it until the player gives
separate production authority and the active boundary is recorded above.
